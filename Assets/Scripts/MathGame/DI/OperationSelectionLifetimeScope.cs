using VContainer;
using VContainer.Unity;

namespace MathGame.DI
{
    public class OperationSelectionLifetimeScope : LifetimeScope
    {
        protected override void Configure(IContainerBuilder builder)
        {
        }
    }
}
